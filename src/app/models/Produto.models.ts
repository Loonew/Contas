export interface Product {
    id: string;
    nome: string;
    preco: string;
    qtd: number;
    user_create: string;
    user_edit: string;

}

//se eu quisesse que um campo fosse opcional, colocaria um ? entre a variável e os dois pontos